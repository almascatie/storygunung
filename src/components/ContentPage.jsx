import React, { useState } from 'react'
const unavailable = { title: 'Referensi', intro: 'Data referensi belum tersedia dalam repository ini.', sections: [] }
export function ContentPage({ kind, data }) { const content = data || unavailable; return <section className="content-page"><div className="content-hero"><span className="eyebrow">GUNUNG IYA · ENDE</span><h1>{content.title}</h1><p>{content.intro}</p></div>{content.timeline && <div className="timeline">{content.timeline.map((item, i) => <article key={i}><span>{item.period}</span><h2>{item.title}</h2><p>{item.description}</p></article>)}</div>}{content.purpose && <article className="content-card"><h2>{content.purpose.title}</h2><p>{content.purpose.content}</p></article>}{content.approach && <article className="content-card"><h2>{content.approach.title}</h2><p>{content.approach.content}</p></article>}{content.sections?.map((item, i) => <article className="content-card" key={i}><h2>{item.title}</h2><p>{item.content}</p></article>)}</section> }
